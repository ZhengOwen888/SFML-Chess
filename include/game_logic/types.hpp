#ifndef GAMELOGIC_TYPES_HPP
#define GAMELOGIC_TYPES_HPP

namespace GameLogic
{
    // global color enum in GameLogic namespace
    enum class Color
    {
        DARK,
        LIGHT,
        NONE
    };

    // other globals or global enums to be deined later:
    // PieceType, BOARD_SIZE, Direction ( there are 8 ), MoveType
}

#endif